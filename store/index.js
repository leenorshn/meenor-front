export const state = () => ({
    counter: 0
  })
  
  export const mutations = {
    increment(state) {
      state.counter++
    }
  }
  
  export const actions={
      login(context,data){

      },
      register(context,data){

      }
  }